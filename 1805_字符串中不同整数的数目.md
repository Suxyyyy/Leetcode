```cpp
class Solution {
public:
    int numDifferentIntegers(string s) {
        string tmp="";
        s+='e';
        set<string>ss;
        for(char c : s) {
            if(!(c>='0' && c<='9')) {
                if(!tmp.empty()) {
                    ss.insert(tmp);
                    tmp.clear();
                }
            }
            else {
                if(tmp=="0") tmp.clear(); //前导0
                tmp+=c;
            }
        }
        return ss.size();
    }
};
```

 [1805. 字符串中不同整数的数目](https://leetcode.cn/problems/number-of-different-integers-in-a-string/)  

📝 核心笔记  

**大数处理**：题目中的整数可能非常大（超过 `long long` 范围），因此必须以 **字符串** 形式存储。

* **前导零逻辑**：`"001"` 与 `"1"` 视为同一个整数，`"000"` 视为 `"0"`。
* **哨兵技巧**：在字符串末尾添加一个字母，可以避免在循环结束后重复编写 `if (!tmp.empty())`。 
* 📊 复杂度分析   
  *   **时间复杂度**: O(n)    遍历字符串一次需要 O(n)。     哈希表插入操作的平均时间复杂度取决于字符串长度，但在本题约束下整体趋近线性。 
  *   **空间复杂度**: O(n)   最坏情况下（如 "1 2 3 4..."），哈希表和临时字符串需要存储所有字符。